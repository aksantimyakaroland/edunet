/**
 * @fileoverview Bouton résumé IA — désactivé dans la version UOB.
 * L'assistant IA a été retiré du périmètre fonctionnel.
 * @author Roland Myaka
 */
import React from 'react';
// Composant désactivé — assistant IA non utilisé dans Edunet UOB
const SummarizeButton: React.FC<{ textToSummarize: string }> = () => null;
export default SummarizeButton;
